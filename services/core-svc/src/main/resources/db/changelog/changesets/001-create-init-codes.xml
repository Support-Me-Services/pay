<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
                    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                    http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <!-- Raw SQL (nie abstrakcyjne <createTable>/<addUniqueConstraint>) — reguła
         projektu od Fazy 8: unikalność/constrainty na Postgresie zawsze przez
         raw SQL, żeby typ obiektu (CONSTRAINT vs INDEX) był jawny i audytowalny,
         nie zależny od tego, jak Liquibase akurat zdecyduje się go zbudować pod
         danym silnikiem (to dokładnie ten typ niejednoznaczności, który
         spowodował SQLSTATE 42704 w Laravelu — patrz claude/marcin/02-baza-danych.md). -->
    <changeSet id="001-create-init-codes" author="core-svc">
        <sql>
            create table init_codes (
                id                      bigint generated always as identity primary key,
                uuid                    varchar(36) not null unique,
                label                   varchar(255) not null,
                organization_id         bigint,
                owner_user_id           bigint,
                shop_item_id            bigint,
                target_organization_id  bigint,
                active                  boolean not null default true,
                created_at              timestamptz not null default now(),
                updated_at              timestamptz not null default now(),

                constraint init_codes_exactly_one_owner check (
                    (organization_id is not null and owner_user_id is null)
                    or (organization_id is null and owner_user_id is not null)
                )
            );

            create index init_codes_organization_id_idx on init_codes (organization_id);
            create index init_codes_owner_user_id_idx on init_codes (owner_user_id);
        </sql>
        <rollback>
            <sql>drop table if exists init_codes;</sql>
        </rollback>
    </changeSet>
</databaseChangeLog>
