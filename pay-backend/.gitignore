.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar
.kotlin/
out/
*.iml
.idea/

# Uploady lokalnego dev (FileStorageService, storage.private-root/public-root
# domyślnie relatywne do katalogu roboczego modułu app) — nigdy nie commitować.
app/storage/

# Sekrety lokalne (docker-compose + zmienne dla ./gradlew bootRun) — tylko .env.example jest wersjonowany.
.env
